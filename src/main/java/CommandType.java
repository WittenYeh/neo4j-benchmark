/**
 * Defines the set of valid commands that can be executed by the benchmark tool.
 */
public enum CommandType {
    load_graph,
    load_property,
    test_read_write_op,
    test_basic_op,
    test_property,
    test_algm
}